---

export interface Props {
	menuItem: string[];
	href: string;
}

const { href, menuItem } = Astro.props;
---

<a href=`#${menuItem}` class="text-gray-600 hover:text-indigo-300 rounded-md px-3 py-2 text-base font-normal">{menuItem}</a>